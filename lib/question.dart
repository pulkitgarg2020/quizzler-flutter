// 1. Class name always starts with a capital letter
class Question {
  String questionText;
  bool questionAnswer;

  // 1. this format looks a lot like how other functions in
  //    dart looks like
  // 2. CONSTRUCTOR
  // 3. Much simplified constructor
  Question(this.questionText, this.questionAnswer);
}
